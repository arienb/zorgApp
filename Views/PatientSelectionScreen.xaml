<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodels="clr-namespace:zorgApp.ViewModels"
    xmlns:models="clr-namespace:zorgApp.Models"
    x:Class="zorgApp.Views.PatientSelectionScreen"
    x:DataType="viewmodels:PatientSelectionScreenViewModel"
    Title="Patiënten"
    BackgroundColor="#F5F7FA"
    Shell.NavBarIsVisible="True">

    <Grid RowDefinitions="*,Auto">

        <!-- PATIENTEN LIJST -->
        <RefreshView
            Grid.Row="0"
            IsRefreshing="{Binding IsRefreshing}"
            Command="{Binding LoadPatientsCommand}"
            RefreshColor="#6366F1">

            <CollectionView
                ItemsSource="{Binding Patients}"
                SelectionMode="None"
                Margin="0,10,0,0">

                <!-- Empty View -->
                <CollectionView.EmptyView>
                    <StackLayout
                        Padding="40"
                        VerticalOptions="Center"
                        HorizontalOptions="Center">
                        <Border
                            Padding="40,50"
                            BackgroundColor="White"
                            StrokeThickness="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="20" />
                            </Border.StrokeShape>
                            <Border.Shadow>
                                <Shadow
                                    Brush="Black"
                                    Offset="0,4"
                                    Radius="10"
                                    Opacity="0.1" />
                            </Border.Shadow>

                            <VerticalStackLayout Spacing="15">
                                <Label
                                    Text="🧍♂️"
                                    FontSize="80"
                                    HorizontalTextAlignment="Center" />
                                <Label
                                    Text="Nog geen patiënten beschikbaar"
                                    FontSize="20"
                                    FontAttributes="Bold"
                                    TextColor="#1F2937"
                                    HorizontalTextAlignment="Center" />
                                <Label
                                    Text="Gebruik de + knop om een patiënt toe te voegen"
                                    FontSize="14"
                                    TextColor="#6B7280"
                                    HorizontalTextAlignment="Center" />
                            </VerticalStackLayout>
                        </Border>
                    </StackLayout>
                </CollectionView.EmptyView>

                <!-- PATIENT TEMPLATE -->
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Patient">
                        <Border
                            Margin="16,8"
                            Padding="20,16"
                            BackgroundColor="White"
                            StrokeThickness="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="20" />
                            </Border.StrokeShape>
                            <Border.Shadow>
                                <Shadow
                                    Brush="#6366F1"
                                    Offset="0,4"
                                    Radius="12"
                                    Opacity="0.15" />
                            </Border.Shadow>

                            <Grid
                                ColumnDefinitions="*,Auto"
                                RowDefinitions="Auto,Auto"
                                RowSpacing="5">

                                <!-- Patient Info -->
                                <Label
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Text="{Binding Name}"
                                    FontSize="19"
                                    FontAttributes="Bold"
                                    TextColor="#1F2937" />

                                <Label
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Text="{Binding Email}"
                                    FontSize="14"
                                    TextColor="#6B7280" />

                                <Border
    Grid.Row="0"
    Grid.RowSpan="2"
    Grid.Column="1"
    WidthRequest="40"
    HeightRequest="40"
    StrokeThickness="0"
    HorizontalOptions="End"
    VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#EC4899" Offset="0.0" />
                                            <GradientStop Color="#8B5CF6" Offset="1.0" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer
            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PatientSelectionScreenViewModel}}, Path=RemovePatientCommand}"
            CommandParameter="{Binding .}" />
                                    </Border.GestureRecognizers>

                                    <!-- Built‑in MAUI delete/trash icon in white -->
                                    <Image
        WidthRequest="22"
        HeightRequest="22"
        HorizontalOptions="Center"
        VerticalOptions="Center">
                                        <Image.Source>
                                            <FontImageSource
                FontFamily="Microsoft.Maui.Controls.Compatibility"
                Glyph="🗑"
                Color="White" />
                                        </Image.Source>
                                    </Image>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
        </RefreshView>

        <!-- ADD BUTTON -->
        <Border
            Grid.Row="1"
            WidthRequest="70"
            HeightRequest="70"
            Margin="0,0,20,20"
            StrokeThickness="0"
            HorizontalOptions="End"
            VerticalOptions="End">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="35" />
            </Border.StrokeShape>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#6366F1" Offset="0.0" />
                    <GradientStop Color="#8B5CF6" Offset="1.0" />
                </LinearGradientBrush>
            </Border.Background>
            <Border.Shadow>
                <Shadow
                    Brush="#6366F1"
                    Offset="0,6"
                    Radius="20"
                    Opacity="0.4" />
            </Border.Shadow>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding AddPatientCommand}" />
            </Border.GestureRecognizers>

            <Label
                Text="+"
                FontSize="40"
                FontAttributes="Bold"
                TextColor="White"
                HorizontalOptions="Center"
                VerticalOptions="Center" />
        </Border>
    </Grid>
</ContentPage>